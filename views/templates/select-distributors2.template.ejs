<span id="<%= locals.componentId %>" class="pointer">

    <label for="" class="form-label"><%= UI_DISPLAY_LANG.views.pages.global.label31 %></label>
    <input type="text" class="input-text input-text-outline mb-2 border-bottom-forced select_distributors_template_search_input" placeholder="<%= UI_DISPLAY_LANG.views.pages.global.placeholder5 %>">
    <select name="" class="input-text" id="<%= locals.selectId ? locals.selectId : 'distributor_select' %>">
        
    </select>

</span>

<script>

    $(async function()
    {
        const eventsReceiverId = `<%- locals.eventsReceiverId %>`
        
        const componentId = `<%- locals.componentId %>`
        const component = $('#'+componentId)

        var options = checkJSON(`<%- (locals.options) ? JSON.stringify(locals.options) : '' %>`)
        const defaultOptions = {
            search: {
                currentAdministrationOnly: true
            }
        }

        options = {...defaultOptions, ...options}
        
        var selectId = (`<%- locals.selectId %>` == '' || `<%- locals.selectId %>` == null) ? 'distributor_select' : `<%- locals.selectId %>`
        var selectionEventName = (`<%- locals.selectionEventName %>` == '' || `<%- locals.selectionEventName %>` == null) ? 'distributor-selected' : `<%- locals.selectionEventName %>`

        var select_distributors_template_search_input = component.find('.select_distributors_template_search_input')

        var distributor_select = component.find(`#${selectId}`)

        detectTypingEnd(select_distributors_template_search_input, () =>
        {
            select_distributors_template_displayAll()
        }, 500)
        // select_distributors_template_search_input.on('keyup', select_distributors_template_displayAll)
        // display all
        select_distributors_template_displayAll()
        async function select_distributors_template_displayAll()
        {
            distributor_select.html(`<option value=""><%= UI_DISPLAY_LANG.views.pages.global.placeholder11 %></option>`)

            const params = {
                query: select_distributors_template_search_input.val(),
                advanced: {
                    administration_id: (options.search.currentAdministrationOnly) ? USER_CONFIG.administration.clinicId : null,
                    employee_type_code: EMP_TYPE_DISTRIBUTOR,
                }
            }

            if ( options.search.hasOwnProperty('administration_id') )
            {
                params.advanced.administration_id = options.search.administration_id
            }
            else
            {
                params.advanced.administration_id = (options.search.currentAdministrationOnly) ? USER_CONFIG.administration.clinicId : null
            }

            var res = await EMPLOYEE_MODEL.advancedSearch(params)

            if ( res.code == 404 ) return

            // dispatch event
            
            var data = res.data
            var html = ''

            $.each(data, (k,v) =>
            {
                var data = btoa( encodeURIComponent(JSON.stringify(v)) )
                html += `<option value="${v.employee_id}" data-object="${data}">${v.employee_name}</option>`
            })
            // add html
            distributor_select.append(html)

            if ( isNull(distributor_select.find(':selected').val()) ) return
            dispatchCustomEvent(selectionEventName, { 
                distributor: {
                    id: distributor_select.find(':selected').val(),
                    name: distributor_select.find(':selected').text(),
                    data: checkJSON( decodeURIComponent( atob( distributor_select.find(':selected').data('object') ) ) ),
                },
                eventsReceiverId: eventsReceiverId 
            }, distributor_select[0])
        } 
    })

</script>